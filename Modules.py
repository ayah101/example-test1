import Utils

numbers = [10, 3, 4, 7, 4]
max = Utils.find_max(numbers)
print(max)


print('Imported my_module')
test = 'Test String'


def find_index(to_search, target):
    # find the index of value in sequence
    for i, value in enumerate(to_search):
        if value == target:
            return i

            return -1


# Scope of vAriables
# class variables extracting
# calling methods/ importing
# DAta Types
# Spacing / Indenting
